#/bin/bash 

# TODO: add coroutine to this so it reruns every once in a while when the cli tools is open.
docker images > image_info.txt
echo "Image info saved to image_info.txt"#/bin/bash 

# TODO: add coroutine to this so it reruns every once in a while when the cli tools is open.
docker images > image_info.txt
echo "Image info saved to image_info.txt"

